import React, { ReactElement } from "react";
import { DesktopIcon } from "components";

const GithubdesktopIcon: React.FC = (): ReactElement => {
  return <DesktopIcon iconUrl="/images/github-logo.png">Github</DesktopIcon>;
};

export default GithubdesktopIcon;
