import React, { ReactElement } from "react";

const TitleBarControllers: React.FC = (): ReactElement => {
  return <div className="flex flex-row gap-1">s</div>;
};

export default TitleBarControllers;
